#!/usr/bin/env bash
#
# Accepts a .c or .cpp or .cc file, compile it and execute it
#
# It avoids me to create a Makefile file

filename="${1%.*}"
FLAGS="-Wall -Wextra -Werror -O2 -g"
CFLAGS="${FLAGS}" CXXFLAGS="${FLAGS} -std=gnu++11" make ${filename} && ./${filename}

