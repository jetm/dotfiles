# shellcheck disable=SC2148
#
# All keybindings have been consolidated into 09-zvm-init.zsh
#
# Widget definitions live at file scope (so they're available early).
# All bindkey calls live inside zvm_after_init() to prevent zsh-vi-mode
# from overriding them.
#
# See: 09-zvm-init.zsh

# vim:set ft=zsh ts=2 sw=2 et:
