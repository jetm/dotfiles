# Fish shell options

# Disable greeting (cleaner than empty fish_greeting function)
set -g fish_greeting

# Vi mode escape delay: lower = snappier Escape → normal mode (default 300ms)
set -g fish_escape_delay_ms 10

# Vi selection mode: inclusive matches Vim behavior
set -g fish_cursor_selection_mode inclusive
